using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AchievementManager : MonoBehaviour
{
    public bool IsLv1Complete = false;
    public bool IsLv2Complete = false;
    public bool IsLv3Complete = false;
    public bool IsAllComplete = false;
    public bool IsAllComplete1 = false;
    public bool IsLvController = false;
    public bool IsLava = false;
    public bool IsStar = false;
    public bool IsFlip = false;
    public bool IsSomething = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
